from __future__ import annotations

"""Markdown 清洗策略（预留占位，Phase 1 暂不启用）。"""


def clean_markdown(text: str) -> str:
    """清洗 Markdown 语法（后续实现）。"""
    raise NotImplementedError("Phase 1 暂未实现 Markdown 专用清洗")
